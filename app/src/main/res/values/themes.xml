<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.VIA" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.VIA" parent="Base.Theme.VIA" />

    <style name="CustomAlertDialogTheme" parent="Theme.AppCompat.Dialog.Alert">
        <!-- Applies a dark background with the rounded corners -->
        <item name="android:windowBackground">@drawable/dialog_rounded_bg</item>

        <!-- Changes the title text color -->
        <item name="android:textColorPrimary">@color/text</item>

        <!-- Changes the message body text color -->
        <item name="android:textColorSecondary">@color/text</item>
    </style>

</resources>